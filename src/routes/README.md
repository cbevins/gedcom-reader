# src/routes Svelte App

This little SvelteKit app demonstrates how to:

- upload a settings JSON file from the local file system,
- edit it, then
- save the changed settings and download them back to the client device.